export default {
  base: '/start-homepage/',
  publicPath: '/start-homepage/',
  plugins: [
    'umi-plugin-gh-pages',
  ],
  // publicPath: process.env.NODE_ENV === 'development' ? '/public/' : './', 
}
